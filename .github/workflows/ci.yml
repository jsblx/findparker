name: CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests
        run: npm run test

      - name: Build
        run: npm run build

      - name: Install Playwright browsers (Chromium)
        run: npx playwright install --with-deps chromium

      - name: E2E tests
        run: npm run test:e2e

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

# Future addition, not wired up yet: a Supabase-backed E2E job that runs `supabase start`
# (via the Supabase CLI, using Docker-in-Docker or a services: block) and points
# VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY at the local stack, so the deferred scenarios in
# tests/e2e/README.md - offline-queue resync, cross-user corroboration, multi-device realtime -
# can run as real E2E coverage instead of living only in unit tests + the human test plan. This
# needs Supabase project secrets/service wiring that isn't configured in this repo yet.
#
#  supabase-e2e:
#    runs-on: ubuntu-latest
#    if: false # enable once Supabase CLI + secrets are configured for this repo
#    steps:
#      - uses: actions/checkout@v4
#      - uses: actions/setup-node@v4
#        with:
#          node-version: 22
#          cache: npm
#      - run: npm ci
#      - uses: supabase/setup-cli@v1
#      - run: supabase start
#      - run: npx playwright install --with-deps chromium
#      - run: npm run test:e2e
#        env:
#          VITE_SUPABASE_URL: http://localhost:54321
#          VITE_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_LOCAL_ANON_KEY }}
